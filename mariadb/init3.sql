STOP SLAVE;
RESET MASTER;


CHANGE MASTER TO
  MASTER_HOST='mariadb_node_one',
  MASTER_USER='root',
  MASTER_PASSWORD='password',
  MASTER_PORT=3301,
  MASTER_CONNECT_RETRY=10,
  MASTER_USE_GTID = slave_pos;

START SLAVE;
